commit="Publish $(date +%Y-%m-%d)"
git stash
git checkout develop
stack build
stack exec site build
git fetch --all
git checkout -b master --track origin/master
cp -a _site/. .
git add .
git commit -m "${commit}"
git push origin master:master
git checkout develop
git branch -D master
git stash pop
